(ns stream-processing.core-test
  (:require [clojure.test :refer :all]
            [stream-processing.core :refer :all]))

(deftest score-test
  (testing "FIXME, I fail."
    (is (= 1 (score "{}")))))
