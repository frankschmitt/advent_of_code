(ns stream-processing.core
  (:gen-class))

(defn score
  "Computes and returns the score for an input string"
  [input]
  (+ 0 1))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
